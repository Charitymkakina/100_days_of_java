# Interfaces
`Interface` is a blueprint for a class that specifies a set of abstract methods that implemeting classes must define.

`interface` supports multiple inheritance like behaviours.

Note: To implement multiple interfaces, separate them with a comma

